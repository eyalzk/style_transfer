from __future__ import absolute_import
from __future__ import division
from __future__ import print_function
from scipy import misc
# import tensorflow as tf
import numpy as np
import matplotlib.pyplot as plt
import matplotlib.image as mpimg
import vgg19
import utils
# aaa= np.array([1,2,3],[4,5,6])
# print(aaa.shape)
# w_cont = tf.constant(np.array([1, 1, 1, 1, 1]) / 5.0,dtype="float32")
# # a = tf.constant(3.0,dtype="float32")
# # b = tf.multiply(tf.constant(w_cont[1],dtype="float32"),a)
# a = tf.reshape(w_cont,(-1,1))
# c = tf.matmul(tf.transpose(a),a)
# # print()
# with tf.Session() as sess:
#     print(b.eval())
# print([x for x in range(4)])
# a = [x for x in range(4)]
# print(a)
# a,b,c,d = [x for x in range(4)]
# print(b)
# print(tf.__version__)
x = [1,2,3,4,5]
print(x[:-1])
